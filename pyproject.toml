[project]
name = "quietlink"
version = "0.1.0"
description = "WebRTC 局域网屏幕共享系统，支持音视频传输"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.128.5",
    "python-multipart>=0.0.22",
    "uvicorn>=0.40.0",
    "websockets>=16.0",
]
